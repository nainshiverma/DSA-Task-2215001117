public class MERGE_SORT_ARRAY{
    public static void main(String [] args){

    }
    public void sort(arr){
        if(arr.length <=1){
            return arr;
        }
        else{
            mid = arr.length/2;
            int[]left_half = Arrays.copyOfRange(arr , 0 , mid);
            int[]right_half = Arrays.copyOfRange(arr , mid+1 , length-1);
        }
    }
}